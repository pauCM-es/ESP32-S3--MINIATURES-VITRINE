#include "AmbientLightMode.h"
#include "hardware/ModeManager.h"

namespace Modes {

void ambient_allLights(ModeManager& manager) {
    manager.showStatus("Ambient", "All lights: TODO");
}

void ambient_random(ModeManager& manager) {
    manager.showStatus("Ambient", "Random: TODO");
}

void ambient_rainbow(ModeManager& manager) {
    manager.showStatus("Ambient", "Rainbow: TODO");
}

} // namespace Modes
